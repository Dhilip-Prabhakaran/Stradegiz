# Recorder-only dependencies.
#
# Deliberately excludes fastapi/uvicorn: the Kotak Neo SDK pins ancient
# transitive deps (websockets==8.1, asyncio==3.4.3, urllib3==1.26.14) that
# conflict with uvicorn[standard]. Isolating the recorder in its own image
# keeps those pins away from the API image entirely.
psycopg[binary]>=3.2
psycopg-pool>=3.2
httpx>=0.27

# Kotak Neo SDK ships only from GitHub, not PyPI.
neo_api_client @ git+https://github.com/Kotak-Neo/Kotak-neo-api-v2.git@v2.0.2
